const producto = ' Monitor de 45 pulgadas'

// repeat va a permitir repetir una cadena de texto

const texto = producto.repeat(3);

console.log(texto);

//split te permite dividir un string

console.log(producto.split(' '));