// Sintaxis similar a la de css. el '.' para las clases y el '#' para los id. Retorna todos los elementos que 
//cumplan con el selector


const card = document.querySelectorAll('.card')

console.log(card)

const formulario = document.querySelectorAll('#formulario')
console.log(formulario)